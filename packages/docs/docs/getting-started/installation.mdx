---
sidebar_position: 1 
---
import BrowserOnly from '@docusaurus/BrowserOnly';

# 설치

Edim 을 사용하기 위해서는 먼저 ProseMirror 를 설치해야 합니다.

```bash npm2yarn
npm install prosemirror-state \
            prosemirror-model \
            prosemirror-view
```

Edim 은 각 구성요소가 여러개의 패키지로 나뉘어 배포됩니다.
 
```bash npm2yarn
npm install @edim/core \
            @edim/paragraph \
            @edim/heading \
            @edim/flat-list \
            @edim/menubar
```

```typescript
import { EditorState } from 'prosemirror-state';
import { EditorView } from 'prosemirror-view';

import {
  EDIM_DOC_NODES,
  EDIM_TEXT_NODES,
  createEdimCorePlugins,
} from '@edim/core';
import {
  EDIM_PARAGRAPH_NODES,
  createEdimParagraphKeymapPlugins,
} from '@edim/paragraph';

const state = EditorState.create({
  schema: {},
  plugins: [

  ],
});
```

<BrowserOnly fallback={<div>loading</div>}>
  {() => {
    const { Edim } = require('@site/src/components/editor/edim');
    return <Edim></Edim>;
  }}
</BrowserOnly>